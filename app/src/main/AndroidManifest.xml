<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sankalp.aigen">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".emailSeller"
            android:theme="@style/AppTheme.CustomTheme"></activity>
        <activity
            android:name=".carInfoAndContact"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".CreateAds"
            android:label="@string/title_activity_create_ads"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".genDataOnce" />
        <activity
            android:name=".carsMainUI"
            android:label="@string/title_activity_cars_main_ui"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>